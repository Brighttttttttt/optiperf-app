// Fichier temporaire : reproduit l'incident #44 pour vérifier que les
// parcours connectés le détectent. Sera retiré avant le merge.
export default function Loading() {
  return <p>Chargement…</p>;
}
